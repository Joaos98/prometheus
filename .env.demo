# The public demo: no backend at all. The Household is kept in the browser's own storage,
# and a browser with nothing stored is given the sample Household by running the seed.
# Everything above the storage port is identical to the self-hosted build.
VITE_DEMO=true
